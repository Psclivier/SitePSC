{% extends 'base.html.twig' %}

{% block body %}
    <br>
    <br>
    <br>
    {{ form_start(form_registration) }}
    {{ form_widget(form_registration) }}
    <button type="submit" class="btn btn-success">Valider</button>
    {{ form_end(form_registration) }}
{% endblock %}